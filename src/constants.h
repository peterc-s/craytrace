#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <stddef.h>

typedef enum {
    ERROR,
    SUCCESS,
} Result;

extern const size_t SPHERE_LIST_DEFAULT_SIZE;
extern const size_t IO_BUFSIZ;

extern const double PI;

#endif
